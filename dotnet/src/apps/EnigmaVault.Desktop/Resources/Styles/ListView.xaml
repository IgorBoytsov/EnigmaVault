<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:attach="clr-namespace:EnigmaVault.Desktop.Resources.Attachable">

    <!-- ╔═════════════════════════════════════════════════════════════════════════════╗ -->
    <!-- ║                                   Базовый                                   ║ -->
    <!-- ╚═════════════════════════════════════════════════════════════════════════════╝ -->

    <Style x:Key="ListView.App" TargetType="ListView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Margin" Value="5,5,5,5"/>
    </Style>
    
    <Style x:Key="ListView.ItemContainerStyle" TargetType="ListViewItem">
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="0,3,0,0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="5,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="mainBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            Margin="{TemplateBinding Margin}"
                            SnapsToDevicePixels="true"
                            CornerRadius="{Binding Path=(attach:ControlHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="MouseRightButtonUp">
                                <b:InvokeCommandAction Command="{Binding Path=(attach:ControlHelper.RightClickCommand), RelativeSource={RelativeSource TemplatedParent}}" CommandParameter="{Binding}"/>
                            </b:EventTrigger>
                        </b:Interaction.Triggers>

                        <ContentPresenter/>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1E2F3A"/>
                <Setter Property="BorderBrush" Value="#0095F6"/>
                <Setter Property="BorderThickness" Value="5,0,0,0"/>
                <Setter Property="Foreground" Value="#50A7E7"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#1E2F3A"/>
                <Setter Property="BorderBrush" Value="#0095F6"/>
                <Setter Property="BorderThickness" Value="5,0,0,0"/>
                <Setter Property="Foreground" Value="#50A7E7"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ListView.GroupItemStyle" TargetType="GroupItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupItem">
                    <Grid Margin="{TemplateBinding Margin}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <ToggleButton x:Name="ExpanderButton" 
                                      IsChecked="True"
                                      Cursor="Hand"
                                      Background="{TemplateBinding Background}"
                                      Padding="{TemplateBinding Padding}">
                            <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="3"
                                                        Margin="{TemplateBinding Padding}" 
                                                        SnapsToDevicePixels="True">
                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ToggleButton.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Path x:Name="ArrowPath" Fill="White" Margin="13,0,0,0" VerticalAlignment="Center">
                                    <Path.Style>
                                        <Style TargetType="Path">
                                            <Setter Property="Data" Value="M 0 0 L 4 4 L 8 0 Z"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="False">
                                                    <Setter Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Path.Style>
                                </Path>

                                <WrapPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18" Foreground="#A0D0FF" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ItemCount, StringFormat=' ({0})'}" FontSize="18" Foreground="#88FFFFFF" Margin="8,0,8,0" VerticalAlignment="Center"/>
                                </WrapPanel>
                            </Grid>
                        </ToggleButton>

                        <ItemsPresenter x:Name="ItemsPanel" Grid.Row="1"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsChecked, ElementName=ExpanderButton}" Value="False">
                            <Setter TargetName="ItemsPanel" Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>